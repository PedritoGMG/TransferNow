package com.transfernow.TransferNow;

import com.transfernow.TransferNow.model.File;
import com.transfernow.TransferNow.model.Folder;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class TransferNowApplication {

	public static void main(String[] args) {
		SpringApplication.run(TransferNowApplication.class, args);
	}

}
